{
  "name": "@local-terminal/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo 'No lint for gateway'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/websocket": "^11.2.0",
    "@local-terminal/security": "0.1.0",
    "@local-terminal/shared": "0.1.0",
    "fastify": "^5.2.1",
    "nanoid": "^5.1.5",
    "node-pty": "^1.1.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "supertest": "^7.1.0",
    "tsx": "^4.19.3",
    "vitest": "^3.0.9"
  }
}
